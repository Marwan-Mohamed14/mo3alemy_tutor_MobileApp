// Handles req/res: parses input, calls authService, shapes the HTTP response.
// No Supabase or business-rule code belongs here — that lives in the service/repository.
export const authController = {};
