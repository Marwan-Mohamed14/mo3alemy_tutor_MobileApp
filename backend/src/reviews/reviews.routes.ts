import { Router } from 'express';

export const reviewsRouter = Router();

// Wire route handlers here, e.g.:
// reviewsRouter.post('/', reviewsController.create);              // service must verify QR check-in happened
// reviewsRouter.get('/tutor/:tutorId', reviewsController.listForTutor);
