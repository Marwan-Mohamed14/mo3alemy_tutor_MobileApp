import { Router } from 'express';

export const tutorsRouter = Router();

// Wire route handlers here, e.g.:
// tutorsRouter.get('/', tutorsController.search);          // filter by subject/curriculum/price/rating
// tutorsRouter.get('/:id', tutorsController.getProfile);    // public professional profile
// tutorsRouter.put('/me', tutorsController.updateOwnProfile); // tutor edits own profile
