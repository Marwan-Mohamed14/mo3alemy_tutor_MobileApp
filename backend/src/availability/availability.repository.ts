// Only place in this module allowed to call `supabase` directly.
// Runs queries and maps raw rows to the types in ./availability.model.
//
// import { supabase } from '../supabase/client';
export const availabilityRepository = {};
