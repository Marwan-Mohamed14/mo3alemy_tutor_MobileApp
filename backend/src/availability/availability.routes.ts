import { Router } from 'express';

export const availabilityRouter = Router();

// Wire route handlers here, e.g.:
// availabilityRouter.get('/:tutorId', availabilityController.listSlots);
// availabilityRouter.post('/', availabilityController.addSlot);
// availabilityRouter.delete('/:id', availabilityController.removeSlot);
// availabilityRouter.put('/buffer', availabilityController.setBuffer); // per-subject session buffer
